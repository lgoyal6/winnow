name: Deploy recorded replay
on:
  push:
    branches: [main]
    paths: ["public-proof/**", "web/public/fixtures/demo-transcript.json", ".github/workflows/pages.yml"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: cp web/public/fixtures/demo-transcript.json public-proof/demo-transcript.json
      - run: node scripts/verify-public-replay.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: public-proof
      - id: deployment
        uses: actions/deploy-pages@v4
